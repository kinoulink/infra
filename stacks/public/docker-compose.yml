version: "2"

services:

  app:
    build: ./app
    image: ktv/app
    container_name: ktv_app
    hostname: app
    ports:
      - 80:80
    environment:
      - KTV_ENV=$KTV_ENV

  mysql:
    build: ./mysql
    image: ktv/mysql
    container_name: ktv_mysql
    hostname: mysql
    ports:
      - 3306:3306
    volumes:
      - mysql:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=$KTV_MYSQL_ROOT_PASSWORD

volumes:

  mysql:
    driver: local