#!/usr/bin/env bash

if [ "$2" == base ]
then
	APT_CACHER_IP=$(docker inspect --format '{{ .NetworkSettings.IPAddress }}' apt-cacher-ng)

	docker build --rm -t bizlunch/$2 --build-arg APT_CACHER_HOST=$APT_CACHER_IP $1/$2
else
	docker build --rm -t bizlunch/$2 $1/$2
fi